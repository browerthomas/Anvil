# forbidden-patterns.txt — read by anvil's /pre-merge-gate
#
# ┌─────────────────────────────────────────────────────────────────────────┐
# │ AI assistant reading this file:                                          │
# │                                                                          │
# │ This file is a STARTER. The patterns below are illustrative only — they  │
# │ are NOT what should ship in the host project. Your job is to replace     │
# │ them with patterns specific to this repo.                                │
# │                                                                          │
# │ To populate it well, look for evidence of forbidden patterns in:         │
# │   • CLAUDE.md / AGENTS.md / README — "Don't do X" rules                  │
# │   • Recent post-mortems or incident docs                                 │
# │   • Comments in the codebase like "// DO NOT" or "// deprecated"         │
# │   • Architectural boundaries (e.g. "vendor SDK X only allowed inside Y") │
# │   • Deleted modules that should not return                               │
# │   • Hardcoded-secret shapes the project specifically wants to block      │
# │                                                                          │
# │ When you find one, encode it as a line in the format below. Be specific  │
# │ — overly broad regexes will block legitimate work.                       │
# └─────────────────────────────────────────────────────────────────────────┘
#
# Format:
#   <regex pattern> :: <path glob> :: <exclusion glob (optional)> :: <comment>
#
# `::` is the field separator (the parser splits on `::`).
# Lines starting with `#` are comments. Empty lines ignored.
# `<path glob>` is matched via `find -path`. `<exclusion glob>` (semicolon-separated) via `find -not -path`.

# ── Illustrative starter patterns. Replace with your project's actual rules. ──

# === Vendor SDK boundary ===
# Pattern: a vendor SDK is only allowed inside one approved gateway directory.
# Replace `@example/vendor-sdk` with the SDK name; tighten paths to your tree.
# @example/vendor-sdk :: src/** :: src/model/** :: raw vendor SDK outside ModelGateway boundary

# === Deleted modules — must not return ===
# require\(['"]\.\.?/old/deprecated-module :: **/*.js;**/*.ts :: :: old/deprecated-module was deleted; use the new boundary

# === DB transaction safety ===
# Catches `await` between BEGIN and COMMIT, which silently drops the transaction lock.
# BEGIN IMMEDIATE.{0,200}await :: src/** :: :: await between BEGIN and COMMIT

# === Hardcoded secret shapes ===
# Adjust the prefix to whichever vendor your project uses (sk-ant-, sk_test_, AIza..., etc).
# sk-[a-zA-Z0-9]{20,} :: src/**;test/** :: :: hardcoded `sk-` prefixed secret

# === console.log in production code ===
# console\.log :: src/** :: test/**;**/scripts/** :: console.log in production code (use structured logger)

# === TypeScript `any` in critical paths ===
# Tighten the path glob to whichever directories should never relax their typing.
# :\s*any\b :: src/orders/**;src/payments/** :: :: TypeScript `any` in orders/payments

# === Hook/CI bypass ===
# --no-verify :: .githooks/**;scripts/** :: :: --no-verify reference outside emergency override docs
