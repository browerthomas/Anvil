# anvil/pre-merge-gate + /grind — known flake patterns
#
# ┌─────────────────────────────────────────────────────────────────────────┐
# │ AI assistant reading this file:                                          │
# │                                                                          │
# │ This file is a STARTER. The patterns below are CATEGORY EXAMPLES, not    │
# │ rules to keep. Replace them with the host project's actual flakes.       │
# │                                                                          │
# │ How to find real flakes for this project:                                │
# │   • `git log --all --grep="flake\|flaky\|retry"` for past mitigations    │
# │   • CI history for tests that have failed + passed in the same PR        │
# │   • Project docs / runbooks for "known flake" sections                   │
# │   • Operator memory ("which test do we always have to retry?")           │
# │                                                                          │
# │ One regex per line. Matches against test names AND file paths.           │
# │ Matching test failures get retried ONCE before being treated as real     │
# │ failures. Use sparingly — masking a real bug as "flake" is worse than    │
# │ a noisier signal.                                                        │
# └─────────────────────────────────────────────────────────────────────────┘
#
# Comments start with #. Empty lines ignored.

# ── Illustrative category examples. Replace, don't keep verbatim. ──

# === Test-runner concurrency / cross-worker races ===
# .*\.race\.test\.(ts|js)$              # tests with cross-worker DB or queue races

# === External sandbox eventual-consistency ===
# .*third-party-sandbox.*               # vendor sandbox occasional latency or ordering quirks

# === Browser/E2E flakes ===
# .*screenshot-diff.*                   # subpixel rendering drift
# .*timing-sensitive-animation.*        # CSS animation timing races
# .*playwright.*timeout.*               # Playwright specs that bite under CPU pressure

# === Network-dependent integration ===
# ^integration:.*\.network$             # integration tests that hit the network

# ── End of starter examples. Add your project's real flakes below. ──
